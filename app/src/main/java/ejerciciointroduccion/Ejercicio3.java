package ejerciciointroduccion;

public class Ejercicio3 {
  /*
   * Multiplicar matrices
   * @param matriz a (int[][])
   * @param matriz b (int[][])
   * @thows RuntimeException si las matrices no se pueden multiplicar
   * @return matriz resultante (int[][])
   */
   public int[][] multiplicarMatrices(int a[][], int b[][]) throws RuntimeException{
    int filasA = a.length;
    int filasB = b.length; 
    int columnasA = a[0].length;
    int columnasB = b[0].length;

    throw new UnsupportedOperationException("Not supported yet.");

   }
}
